# Synthetic fixtures

Fixtures are project-owned, deterministic HTML documents served over HTTP for
Engine and Official Distribution validation. They must not contain copied
production pages, private URLs, credentials, personal data, or proprietary
assets.

Run `pnpm dev:fixtures`, then open `http://127.0.0.1:4173/smoke/`.
